//
   Created by mario on 4/14/2016.
extends ../shared/layout
block content
    .row
        if employees
            form.col.s12()
                .row
                    .input-field.col.s6
                        select#employees.validate(name="employees")
                            option(value="" disabled selected) Choose the employee
                            each employee in employees
                                option(value="#{employee._id}") #{employee.name+" "+employee.lastName+" "+employee.secondLastName}
                    .input-field.col.s6
                        input#absences.validate( name="absences" type='number' min="0" )
                        label(for='absences') Absences
                .row
                    button.btn.waves-effect.waves-light(type='submit', name='action')
                        | Submit
                        i.material-icons.right send
        else
            .col.s12.center-align
                a.waves-effect.waves-light.btn-large(href="/employee")
                    i.material-icons.right perm_identity
                    | First thing first, please add an employee




